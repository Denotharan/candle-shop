Backend
=======

FastAPI backend for Serein.

Run it with:

```powershell
pip install -r requirements.txt
python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

The API stores local development data in `data.json`.
